api:
  inventory:
    base: /api/inventory/v1
    check: /check
    reserve: /reserve
    reduce: /reduce
    restore: /restore
    add: /add
    get-by-product: /product/{productId}
    get-all: /all